# .env
# Configurações atuais do sistema

# ------------------------------------------------------------
# Configurações PostgreSQL
# ------------------------------------------------------------
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=rentals_dev
# Porta que será exposta no HOST (diferente de 5432 para evitar conflito local). Ajuste para 5432 se quiser substituir um Postgres local.
DB_HOST_PORT=5433

# URL de conexão utilizada pela aplicação (NestJS / ORM / Prisma etc.)
# Formato: postgresql://USER:PASSWORD@db:5432/DB_NAME
# ------------------------------------------------------------
# Database URL para desenvolvimento 
# ------------------------------------------------------------
DATABASE_URL="postgresql://user:password@db_dev:5432/rentals_dev"

# ------------------------------------------------------------
# API
# ------------------------------------------------------------
PORT=3000
NODE_ENV=development

# ------------------------------------------------------------
# JWT / Autenticação
# ------------------------------------------------------------
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production

# ------------------------------------------------------------
# Timezone
# ------------------------------------------------------------
TZ=UTC

# ------------------------------------------------------------
# Porta da API (mapeada no docker-compose para 3000:3000)
# ------------------------------------------------------------
PORT=3000

# ------------------------------------------------------------
# NODE_ENV (development | production | test)
# ------------------------------------------------------------
NODE_ENV=development

# ------------------------------------------------------------
# Exemplo de variável para JWT / Autenticação (remova se não usar)
# ------------------------------------------------------------
JWT_SECRET=change-me

# ------------------------------------------------------------
# Healthcheck endpoint configurado no compose: /health (não alterar sem atualizar docker-compose.yml)
# ------------------------------------------------------------

TZ=UTC
