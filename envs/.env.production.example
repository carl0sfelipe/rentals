# Exemplo de variáveis de ambiente para produção
# Copie este arquivo para .env.production e configure as variáveis

# Database (Railway PostgreSQL ou outro provedor)
DATABASE_URL="postgresql://usuario:senha@host:5432/database_name?schema=public"

# JWT Secret (gere uma chave secreta forte)
JWT_SECRET="sua-chave-jwt-super-secreta-aqui"

# Node Environment
NODE_ENV="production"

# Port (Railway vai definir automaticamente)
PORT=3000

# CORS Origins (domínios permitidos)
CORS_ORIGINS="https://seu-frontend.vercel.app,https://seu-dominio.com"

# Unsplash (opcional - para imagens)
UNSPLASH_ACCESS_KEY="sua-chave-unsplash-opcional"
